all: clean onBoardControl

onBoardControl:
	gcc -std=c99 -iquote ../net -iquote ../EBU_packets -o onBoardControl onBoardControl.c ../EBU_packets/EBUanalogOut.c ../EBU_packets/EBUrelays.c

clean:
	rm -rf *.o onBoardControl
