tweakingOfSensorData is a function used when switching between IMUs, and when building new ones,
to see what constant value needs to be removed to get a nice bias. 

Can also be used just to print out the values from the IMU on screen, either raw data or bias corrected. 
